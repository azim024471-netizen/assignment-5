 <!-- ANS TO THE QUE NO 1 -->
  var :
   var is function scop. It dosen't respect block scop. 
   For hoisting var give unexpected bugs.

   let:
   let is block scoped. lets variables value can be change. let also hoiste but it go to
   Temporal Dead Zone and give error.

 const : 
     const is  Block scoped
      const   value cannot be reassigned. const  is also hoistable and store in Temporal Dead Zone give us error.

<!-- ANS TO THE QUE NO 2 --> 

 spread operator (...) :
   
   Spread operator is ES6 methood of javascript.It is veru usefull. 
   It use to merge multiple arrays  and objects. 
   Spread operator makes easier to pass values into a function. It is very usefull for copying and updating objects.  



 <!-- ANS TO THE QUE NO 3 -->
  
  map() :
  Map() is powerful array method of javascript. Creates a new array. 
  Transforms every element in the array. Returns the same number of elements as the original array.

  filter() :
Filter is another exiting arry method of javascript.Its creates a new array.
Returns only the elements that match a condition like filterig something. Its use when want to 
select specific elements from an array

forEach()

Foreach is useful array method for looping in an arry. Its does not return a new array.
We use forEach() method for side effects like printing or updating values.One of easiest way for 
looping in an arry. 



 <!-- ANS TO THE QUE NO 4 -->

  arrow function : 
  An arrow function is a shorter and modern way to write functions in JavaScript.  It is the 
  shortest version of traditonal function where we can write it () => ()



 <!-- ANS TO THE QUE NO 5 -->

 Template literals :
 Template literals is powerful method of javascript. Where we can create strings 
 using backticks ` ` instead of single ' ' or double " " quotes.
 It make easier to include variables and expressions inside strings  by ${}.
 Its very useful for method.